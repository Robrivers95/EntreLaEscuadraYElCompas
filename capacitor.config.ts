import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.masonica.entrelaescuadraycompas',
  appName: 'Entre La Escuadra y El Compás',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
